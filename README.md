# hello-pr

A tiny practice project for learning the GitHub PR workflow.

## What's here

`src/math.js` exports two small functions:

- `add(a, b)` — returns the sum of two numbers
- `subtract(a, b)` — returns the difference of two numbers

## Running tests

```bash
npm test
```
